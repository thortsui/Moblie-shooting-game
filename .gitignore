_test_person.jpg

# 訓練權重不進 web repo（.pt 給 DGX 訓練用；.onnx 待整合後再視需要納入）
models/*.pt
models/*.onnx

# 隱私資料：所有不能上 git 的敏感檔案都放這裡，整個資料夾不同步
隱私資料/

# 訓練資料集（大型二進位，不上 web repo）
訓練資料/

_bench/

# 原始素材照片（用去背後的 assets/gun.png）
IMG_*.jpeg
IMG_*.jpg

# 槍圖原始素材（大型 JPG，不進 git；出處見 assets/guns/sources.md）
assets/guns_raw/

# 內嵌圖片的總覽頁（~2MB，遵守大檔不進 git 原則）
工作日誌/玩家UI-換槍總覽-*.html
