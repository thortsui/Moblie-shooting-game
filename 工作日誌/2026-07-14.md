# 工作日誌 — 2026-07-14

## 昨日狀態
- r5 上線中（v30）；r6 訓練完但未上線——**官方標註重測顯示 r6 才是真贏家**（0.4878 > r5 0.4643），r5 的進步是假的（詳見 7/13 日誌「真實成績單」）。
- r7（+ATR/P3M 人工標註 6.4 萬張）午夜訓完；訓練中最後 val：自家考卷 mask mAP50 0.949。
- r7 之後 GPU 自動接棒：CrowdHuman 官方框重標 v2 → fashion 混合標註 → r7 官方分數重測。
- EasyPortrait 20,000 張轉換完成（labeled_ep）。
- 檢視文件落地 6/7 項；force push 清歷史待使用者確認執行。

## 本日指示與進度
### 使用者指示（凌晨）
1. **r7 先上線**。
2. **用 Workflow 新增換槍功能**：開始前能換、不同槍不同傷害與射速、找第一人稱圖去背、約 5 種槍。

### ⛔ r7 上線前驗收發現退化，暫停上線待定奪
- r7 訓完（166→80 epochs、自家考卷 0.949）。模型已抓回 models/。
- **紅旗**：cbench 煙霧測試同一張雙人測試圖只偵測到 1 人（r4/r5 皆 2 人）。
- 信心分數對比（同圖、imgsz 256、conf 門檻 0.35）：
  | 模型 | 人1 | 人2 |
  |---|---|---|
  | r5 | 0.903 | 0.742 ✓ |
  | r6 | 0.935 | 0.470 ✓ |
  | **r7** | 0.675 | **0.152 ✗（看不見第二位玩家）** |
- 研判：r7 新增的 2.7 萬張單人肖像/時尚照（ATR+P3M）把信心分布拉偏，**多人場景敏感度崩落**——正是遊戲核心場景。
- 處置：detector-seg.js 已改回 r5（線上維持 r5 不動），**r7 未上線**。
- **r7 官方標註最終分數出爐：0.4657**——官方排名 **r6 0.4878 > r7 0.4657 > r5 0.4643 > r4 0.4641**。r7 官方分略優於 r5 但雙人測試圖信心崩落；**綜合判斷 r6 仍是最佳候選**（官方最高 + 雙人穩過門檻），待使用者定奪上線。
- 教訓（呼應檢視問題 3）：加資料要看「分布」——單人肖像類資料比例過高會犧牲多人偵測。r8 配方需控制半身/單人類的占比或做分層取樣。

### ✅ 換槍功能實作完成（Workflow 子代理，只改工作區、未 commit、版號維持 v30）
- **武器表**（js/game.js `WEAPONS`，5 把定案數值）：手槍 25/1s、步槍 34/1.2s、衝鋒槍 12/0.45s、散彈槍 40/1.5s、狙擊槍 70/2.5s（現行只判剪影內外，一律吃 body 傷害；head 保留給未來部位判定）。
- **主選單選武器 UI**（index.html #weaponBox + main.js renderWeaponList）：5 顆按鈕顯示名稱＋傷害/射速、選中紅框高亮、預設手槍；`running` 後 selectWeapon 直接 return（遊戲中不可換）。
- **射速**：FireControl 改可調 `cooldownMs`；恢復 #fireRing 冷卻進度環（conic-gradient＋CSS mask 只留外圈）。
- **傷害**：射手端算好帶在 `{t:'hit', victim, part, dmg}`；HostNet.applyHit 用 dmg（夾 1~100），沒帶 dmg 退回 RULES.damage[part]——新舊版互通。
- **武器廣播**：`{t:'weapon', weapon}` 走 color 同路徑，大廳玩家列顯示各人武器（僅顯示用）。
- **槍圖**：assets/guns/<id>.png，onerror fallback 回 assets/gun.png（圖檔還在 guns_raw/ 未去背，fallback 已實測生效）。
- Playwright 本機實測：選擇/高亮/換圖 fallback/?solo=1 皆正常，無 JS 錯誤。
- 待辦：guns_raw/*.jpg 去背輸出成 assets/guns/<id>.png 後即自動生效。

### ⚠️ 槍圖去背完成，但 4/5 張素材不堪用（Workflow 子代理）
- 流程照舊例：scp 到 DGX rtm_build 容器，rembg（u2net、CPU）Python API 去背 → 裁切透明邊界 → 長邊縮 520 → scp 回 assets/guns/<id>.png。CLI 缺 filetype/watchdog 模組，改走 Python API 繞過。
- 產出（皆已落地 assets/guns/）：pistol 520×498、rifle 520×347、smg 520×337、shotgun 520×480、sniper 365×520。
- **問題：原始素材是「軍人持槍」第三人稱照片，rembg 把整個人保留下來**，與 assets/gun.png（第一人稱、只有槍）風格完全不同：
  - pistol ✓ 勉強可用：手持左輪特寫，近似第一人稱。
  - rifle ✗：士兵背影為主，槍很小。
  - smg ✗：整個士兵側面，槍模糊還殘留背景煙霧。
  - shotgun ✗：整個士兵＋大片去背破碎痕跡。
  - sniper ✗✗：只剩射手臉部與拳頭，**畫面裡根本沒有槍**。
- 結論：去背管線沒問題，是選圖問題。需重新找「第一人稱視角」或「純槍材質」的 CC0/公有領域素材再跑一次；現有 fallback（assets/gun.png）機制可先撐著。

### 📦 r8 標註鏈完成（GPU 自動接棒收工）
- **CrowdHuman 重標 v2**：933 張（官方框提示 SAM2 + 逐人共識 + 70% 覆蓋閘），比舊法 766 張 +22%。多人遮擋精華，品質優先。
- **fashion 混合標註**：**46,327 張**（62.9k 的 74%）——SAM 人體 ∪ 人工衣物遮罩。抽查驗貨：先前「無頭人」的西裝男現在頭/臉/手完整 ✓。
- r8 彈藥總表：COCO 34,385 + CH2 933 + ATR 17,704 + P3M 9,412 + EP 20,000 + fashion 46,327 ≈ **12.8 萬張**。
- ⚠️ r7 教訓在前：單人/半身類（ATR+P3M+EP+fashion ≈ 9.3萬）遠超多人全身類 → **r8 配方必須分層取樣控制比例**（例：單人類抽 30-40%、多人全身類全量+加權），否則會重蹈 r7 多人敏感度崩落。配方待與使用者定案。

### 🚀 r8 管線啟動（使用者指示：拼接合成多人 + 找多人圖 + 加權多人）
- **合成多人**（`synth_multi.py`）：從 fashion/ATR/P3M（皆有精確遮罩）剪人物 → 貼到 COCO 官方確認無人的背景（`bg_list.txt`）→ 2~5 人/張、高度佔背景 35~90%、遮擋 IoU<0.3、後貼者蓋前者可見輪廓 → 15,000 張。**設人工核可閘**：合成完寫 SYNTH_READY，抽查 check/ 過關後 touch synth_approved 才續跑（防垃圾合成浪費 8 小時訓練）。
- **現成多人**（`cocogt_train2yolo.py`）：COCO train2017 官方人工標註轉 YOLO-seg（排除與自動標重複的圖），ds8 只收其中**多人圖**。
- **加權**（`mkds8.py` 分層取樣）：COCO自動標多人×2、官方GT多人、CH2×3、合成15k；單人控量（fashion 46k→15k、ATR→8k、P3M→5k、EP→6k）。
- 訓練用 r6 贏家配方（強增強 copy_paste=0.3 等）、80 epochs。
- **三關驗收**：自家 1078 val + 官方 GT 2693 + 雙人測試圖信心（r5/r6/r8 對比）。
- 預估：準備 1~2h → 核可 → 訓練 ~8h。監看器已掛。

### 🏆 r8 三關成績單（12:00 交卷；68 epochs 跑滿 7.63h 時限）
| 關卡 | r5(現役) | r6 | r8 |
|---|---|---|---|
| 自家 1,078 val | **0.952** | 0.949 | 0.947 |
| **官方人工標註 2,693** | 0.4643 | 0.4878 | **0.5718** ⭐（+23% vs r5） |
| 雙人圖信心（門檻0.35） | 0.903/0.742 | 0.935/0.470 | **0.858/0.738** ✓✓ |
| 瀏覽器雙人偵測 | 2 人 | — | **2 人** ✓ |
- **多人配方大獲全勝**：官方分數跳升 0.4878→0.5718（歷來最大單輪進步），雙人信心回到 r5 水準且無多餘誤檢。自家考卷微降再次證明它只量「模仿老師」。
- 三招各自貢獻：合成多人 15k + COCO 官方 GT 多人 36k + 分層加權（多人導向 74%）。
- 模型已抓回 models/，cbench 煙霧通過（速度不變 ~25ms）。
- ~~建議 r8 上線待定奪~~ → **使用者定案「上」：r8 已上線（603fa6d，版號 v33）**，首頁煙霧測試無錯誤。r6 上線議題撤案。
- 官方評估腳本補洞：cocogt_eval.py 模型清單原寫死到 r7，r8 手動補測後已入 cocogt_summary.json。

### 🎯 偵測調校 v34（使用者實測回饋：多人標不全、要強化瞄準點、FPS 過低）
- **準星中央加權門檻**（detector-seg.js + seg-worker.js 皆改）：畫面中央 30%（瞄準區）門檻 0.18、往邊緣線性升到 0.30（原固定 0.35）——確保瞄準的人一定被標到，邊緣仍防背景誤判。
- **NMS 0.5→0.6**：重疊的多位玩家不再互吃。
- **WebGPU 主模型 256→192**（?hq 參數可回 256）：手機是運算量瓶頸，推論估 40ms→~23ms；桌機卡開銷無感（實測確認）。seg_r8_192.onnx 進 git。
- 代價（誠實記錄）：放寬後測試圖偵測數 2→3（第三個為低信心候選）——可能出現幽靈目標，實戰若誤判過多可把 SEG_CONF_CENTER 調回 0.22-0.25。
- 上線 4332548，版號 v34。**待手機實測驗證**：FPS 提升幅度、多人標記完整度、幽靈目標頻率。

### 🚀 r9 管線啟動（使用者指示：多人準確率再拉高 + 修局部特寫認不出 + 人工標註優先）
- **🧠 已存長期記憶**（使用者強調「記錄記住」）：`training-data-human-labeled-first`——訓練資料一律人工標註優先，自動標註是最後手段。
- **依鐵則先掃人工標註資料集 → 挖到 LV-MHP-v2**（HuggingFace 鏡像 5.4GB）：25,403 張、每張 2~26 人、**人工逐人實例解析**——「人工×多人」完美教材，下載中（`mhp2yolo.py` 轉換，多人 ×2 加權）。授權：學術研究用，圖只留 DGX。
- **局部特寫合成**（`synth_parts.py`）：修「畫面只有手/腿認不出」——從人工標註集（COCO GT/ATR/P3M）random crop 只切到人 15%~90% 的窗再放大成特寫，標籤=遮罩∩窗（繼承人工品質）。訓練 20k + **獨立評估集 600 張**（從 valgt 合成，r8 vs r9 可量化這項能力）。
- ds9 = ds8 配方 + MHP×2 + 局部特寫 20k ≈ 20 萬（加權後）。
- 訓練：過夜跑到**明早 06:00**（時限 ~16h，估 ~95 epochs），四關評估（自家/官方GT/局部特寫/雙人圖）@192（與 v34 線上解析度一致）。監看器已掛。
- ⚠️ **MHP 鏡像檔損毀**（zip64 位移壞、unzip/python/zip -FF/逐檔搶救全失敗，18,130 條目僅救回 13 檔）→ 放棄該鏡像、已清理。**r9 改無 MHP 備案照跑**（局部特寫+長訓練價值不變）。待辦：LV-MHP-v2 官方 Google Drive 來源需人工下載（記入登入牆清單），補進 r10。

### 🔫 遮罩延續 v35（使用者回饋：開火不要被偵測 fps 拖累）
- 盤點：開火本來就不等偵測（用最近 poses、只受武器冷卻節流）——真正的洞是**單幀漏抓時 poses 被整批換掉，目標瞬間消失**，那槍打空。
- 修法：新幀沒接上的目標（以追蹤 id 比對）沿用上一幀遮罩最多 **600ms**（DET_PERSIST_MS），命中判定永遠有「最近可用」剪影。上線 98ef6ef（v35）。
- r9 訓練進度（同時進行）：epoch 10 完成、自家 val 0.916 爬升中，~11 分/epoch。

### ✅ 第三輪槍圖完成：5 把全部換成第一人稱持槍視角（Workflow 子代理，未 commit、未動版號）
- 前兩輪失敗原因確認：照片素材找不到「持槍者本人視角」且風格難一致。本輪改走 **CC0 3D 模型 + 本機 Blender 自渲染**。
- 素材：Quaternius《Ultimate Gun Pack》（OpenGameArt，**CC0**，40+ 模型一包搞定 5 型，風格一致）；zip 存 `assets/guns_raw/quaternius/`。
- 管線：blender-mcp（本機 Blender 4.2，addon 由指令列自動啟動）→ append .blend 模型 → FPS 相機（槍尾後上方偏左、32mm、沿槍管前視；狙擊槍相機放低讓槍口高於鏡筒）→ EEVEE 透明背景 1400×1650 → Pillow 後製：裁邊＋補透明邊讓**槍口尖端水平置中**（對齊 #muzzleFlash 火光在圖頂中央）→ 長邊 520。
- 槍口偵測坑：狙擊鏡上緣比槍口更高，用「頂部 60 行最左群集」而非「最高點」定槍口。
- 產出（RGBA 透明、逐張目視驗收皆為 FPS 視角）：pistol 515×520（滑套後方+準星朝遠）、rifle 520×420（AK 木護木+導軌後退）、smg 520×421（導軌+長彈匣+摺疊托）、shotgun 520×362（泵動雙管膛朝遠）、sniper 520×328（瞄準鏡+槍機，槍口置中頂端）。
- 授權紀錄已重寫 `assets/guns/sources.md`（CC0 免署名；歷史兩輪素材備查）。
