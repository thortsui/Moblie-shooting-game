# 2026-07-18 工作日誌

## r11 訓練管線（使用者「go go」授權開跑）

### 轉換器戰報（五包人工標註 → YOLO-seg 單類 person）
| 來源 | 轉出 | 備註 |
|---|---|---|
| MHP v2 | 15,402 | 逐人 parsing 聯集→實例 polygon。坑：symlink 用主機絕對路徑容器解不開→改相對路徑 |
| OCHuman | 2,064 | **只收全員有遮罩的圖**（skip_partial 2,667）——部分人只有 bbox 的圖會教假陰性，寧可砍半 |
| LIP | 40,234 | train+val 全轉；連通元件各成實例 |
| AISegment | 34,425 | alpha>127；疊圖乍看吃背景，量化驗 alpha 佔比 0.35~0.7 正常（半身特寫佔滿畫面） |
| Supervisely | 2,662 | masks 同名配對 |

抽查疊圖（check/）全數人工目視過關：MHP 雙人各自貼合、OCHuman 重疊人群正確、AISegment 髮絲級。

### 自動鏈 r11_chain.sh（DGX 容器內執行中）
1. `lip_filter.py`：yolo11x 數人頭，≥2 人即整圖剔除（LIP 只標目標人，路人未標=假陰性風險；偵測器只當門警不碰標註）。坑：ultralytics 批次模式 `r.path` 回傳不可靠 → 改 `zip(batch, results)` 配對。
2. `mkds11.py`：ds9 配方 + 新料。多人加權（COCO auto×2、MHP×2、OCHuman×3、CH2×3、cocogt 僅多人）、單人控量（LIP 抽 8k、AISeg 抽 10k、fashion 12k、ATR 6k、P3M 4k、EP 5k）+ 合成多人 + 局部特寫。val 沿用 ds4 的 1078。
3. 訓練：`yolo11n-seg.pt`、**imgsz=320**（訓練解析度首次跟上推論階梯）、epochs=50、batch=64、patience=12 → `runs/seg_r11`。預估 ~20h。
4. 匯出 192/256/320/384 四種 ONNX + `eval_r11.py`（r9 vs r11 @192/@320 於官方 valgt）。

### 監控
Monitor 每 30 分鐘掃：過濾/組 ds 完成、epoch 進度、Traceback/OOM、CHAIN-DONE。

### 待辦（訓完後）
- 看 eval_r11.json：r11 需在 valgt @192 與 @320 都勝 r9 才有上線資格（r7 式退化則停）。
- 上線流程：seg_r11_*.onnx 進 models/、detector-seg.js 檔名切 r11、版號 +1——**先回報使用者定奪，不自動上線**。

## r11 成績單（訓練 10.5h 完成，比預估快一倍）

valgt=官方 COCO 人工標註 2693 張（真答案）；parts=局部特寫 600 張（遊戲貼臉情境）。mask mAP50：

| 解析度 | r9 valgt | r11 valgt | r9 parts | r11 parts |
|---|---|---|---|---|
| 192 | **0.503** | 0.482 | 0.917 | 0.913 |
| 256 | **0.576** | 0.556 | 0.919 | 0.923 |
| 320 | 0.616 | 0.612 | 0.894 | **0.919** |
| 384 | 0.645 | **0.649** | 0.762 | **0.908** |

- r11（訓練 imgsz=320）甜蜜點整體上移：**384 兩項全勝**（parts 差距懸殊 0.762→0.908，r9 在高解析近距離崩、r11 穩）；320 valgt 打平但 parts +2.5pp；**192/256 反輸 2pp**。
- 訓練途中容器 NVML 失聯（Docker 長壽容器 cgroup 老 bug）→ docker restart rtm_build 解決，eval 重跑。
- **建議：混合部署**——階梯 128/192/256 留 r9、320/384 換 r11（階梯本來就每階換檔，只改模型表零風險）。待使用者定奪。
- seg_r11_320/384.onnx 已抓回本機 models/（未接線、未上線）。

## r12 三階段鏈發車(使用者:「開跑,重點 192 畫質,研究方法,目標 8 成」)

方法研究結論(GitHub/官方文件):
- ultralytics 8.4.92 **原生支援 distill_model 蒸餾參數**(官方 KD 指南;seg 相容但未官方跑分)
- albumentations 裝了會自動掛輕度增強;重度劣化需 patch Albumentations 類別
- copy_paste=0.3 對實例分割有效;close_mosaic=10 已是預設;multi_scale 增益小(MMYOLO 實驗)

三階段(全自動,估 ~15h):
1. **r12n**:yolo11n@192 + 劣化增強(MotionBlur/GaussNoise/ISONoise/ImageCompression/暗光偏置)+ copy_paste 0.3
2. **r12s**:yolo11s@192(容量 4×,運算 3.2 GFLOPs≈n@320)——部署候選 + r13 老師
3. **r13n**:r12n 配方 + distill_model=r12s(蒸餾,推論零成本)
評估:@128/192/256 × valgt/parts,與 r9 同場。匯出各檔 onnx。

⚠️ 8 成目標的誠實預估:valgt(COCO 官方考卷)@192 現況 0.503,nano 模型堆滿技巧估 0.52-0.55,
s 模型估 0.56-0.60——**valgt 上 0.8 超出 nano/small 等級物理極限**(大模型全解析度也才 ~0.7)。
但 parts(遊戲貼臉域)已 0.91+、自家 val 0.95+,遊戲實戰指標早已超過 8 成。跑完看數字說話。
