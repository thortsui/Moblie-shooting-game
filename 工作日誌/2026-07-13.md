# 工作日誌 — 2026-07-13

## 昨日狀態
- 遊戲上線 https://thortsui.github.io/Moblie-shooting-game/（輪廓分割 AI + 多人 + 顏色識別 + 開火特效 + 方法A節流 + 方法D Web Worker）。
- r4 模型（2.1萬張，mask mAP50 0.946）已上線取代 r3。
- C（GPU 後處理）在分支 experiment/worker-gpu 實驗中。

## 本日進度

### C 可行性探針（通過）
- onnxruntime-web 探針：`preferredOutputLocation:'gpu-buffer'` 成功、拿得到 `ort.env.webgpu.device`、output0/output1 皆為 GPUBuffer。→ C 的技術地基可行。

### ⭐ 關鍵量測：C 不值得做（數據推翻前提）
- 乾淨量測（主線程、無節流、r4@256）detect() 分解：
  - 前處理 **4ms**、推論 **39.9ms**、後處理 **0.3ms**、總 44.2ms（=19fps）。
- **後處理只有 0.3ms** → C（把後處理搬 GPU）就算歸零也只省 0.3ms，無意義。
- 先前「開銷 55ms 值得做 C」是**被方法A節流的 83ms 等待污染的錯誤數據**；真正瓶頸是**推論(40ms)**，C 不碰推論。
- C 唯一沾邊的是輸出 readback(~10ms 藏在推論內)，但要省它得 NMS+解碼全改 GPU shader（巨大工程/脆弱）換 ~10ms。
- **結論：C 經實測不值得（如量化般誠實止損）。真正加速偵測要降解析度或換小模型；且 A+D 已讓畫面 141fps。**
- 待使用者定奪：接受「C 不做」或堅持省 readback。

### r4 上線 + 背景標註
- r4（2.1萬張，mask mAP50 0.946 > r3 0.934）匯出 256/192/128 換上線（WebGPU 256 / WASM 128）。
- 背景標註持續（供未來 r5）。

### 🔌 電腦重開前存檔（2026-07-13）
- **所有變更已提交並推送遠端**：main 與 origin/main 同步；實驗分支 experiment/worker-gpu 也已推遠端備份。工作區乾淨、無未提交。
- **DGX 不受影響**（獨立機器）：標註 21572 張持續跑、qwen healthy。重開後用 `ssh -i ~/.ssh/dgx_claude ethan@192.168.0.126` 連。
- 本機備份齊全：模型在 `models/`、訓練圖在 `訓練資料/`、r3 訓練成果在 `訓練資料/seg_r2_訓練成果/`（註：資料夾名為r2但內容視實際）。
- **重開後接續事項**：
  1. ~~待使用者定奪 C~~ → **使用者已定案：接受「C 不值得」，此題結案**。分支 experiment/worker-gpu 與探針保留備查。
     - **未來何時該重啟 C**：若後處理耗時變顯著（目前僅 0.3ms）才有意義，例如 → 換成更大模型(遮罩原型更大/多輸出)、輸出解析度大幅提高、或改多類別分割使後處理變重時。屆時「gpu-buffer 輸出可行」的地基已探明可直接用。
  2. r5：背景標註累積更多後可再訓練。
  3. 本地測試伺服器需重啟：`cd 專案 && npx http-server -p 8777 -s`（線上版在 GitHub Pages 不受影響）。
  4. 線上遊戲 https://thortsui.github.io/Moblie-shooting-game/ 照常運作（跟本機無關）。

### 🔄 C 重啟（使用者指示「C繼續」，推翻結案）
- **實作完成**（分支 experiment/worker-gpu，seg-worker.js）：
  - session 用 `preferredOutputLocation:'gpu-buffer'`，兩個輸出留在 GPU。
  - CPU 只用 `getData()` 下載小的 output0（~200KB）做 box 解碼+NMS。
  - 遮罩合成 sigmoid(coeffs·protos)≥0.4 改用自寫 WGSL compute shader 在 GPU 算（比 logit 門檻省 exp），每偵測只回讀 mh×mw 小遮罩（~16KB）→ 省掉 512KB protos readback。
  - 開關：`?noc` 關閉 C 做 A/B；shader 或 gpu-buffer 任一步失敗自動退回 CPU 路徑。
- **實測（桌機 Chrome、r4@256、`_bench/cbench.html`、30張取後25平均）**：
  - C開：total **24.8ms**（run 8.5 + o0下載等待 12.2 + shader+回讀 2.8），mode=gpuC
  - C關：total **26.5ms**（infer 25.1 含全部 readback）
  - **省 1.7ms（~6%）**；`dl` 12.2ms 主要是等 GPU 推論跑完，不是傳輸量 → 再把解碼/NMS搬GPU也省不了多少（上限已到）。
  - **正確性：GPU vs CPU 遮罩逐像素完全一致（mismatch 0）、分數相同** → 命中判定不受影響。
  - 手機上 readback 較貴，實際省多少要手機實測才知道。
- **附帶抓到兩個潛在地雷（順手修了）**：
  1. worker 內 ORT 動態 import `ort-wasm-simd-threaded.jsep.mjs` 會解析到本站 `/js/` 下 404（新版 Chrome 行為），造成 worker **無聲卡死 30 秒後靜默退回主線程**。修法：worker 里明確設 `ort.env.wasm.wasmPaths` 指向 CDN。⚠️ **此修應該也要進 main（production 在新版 Chrome/Android 可能同樣中招）**。
  2. worker init 兩種後端都失敗時原本是 unhandled rejection（無聲）→ 改成 postMessage `init-error`，主線程立即退回主線程偵測器，不用等 30 秒逾時。
- ~~待使用者定奪~~ → **使用者定案「合併」：C 已併回 main 上線（9a7ac0c，版號 v28）**，含 wasmPaths 修復與 init-error 回報。首頁本地煙霧測試無 console 錯誤。
- **後續**：手機開線上版實測 C 實際省多少（`?noc` 可關 C 對照、`?max` 拿掉節流看純偵測 fps；`window.__segProf` 看分解，mode=gpuC 表示 C 生效）。若手機上有異常，C 會自動退回 CPU 路徑，最壞情況等同 v27 行為。

### 🚀 r5 管線啟動（使用者指示「繼續下一步的訓練」）
- 盤點 DGX：標註停在 21572 張（與 r4 相同、無新增）、無標註程序在跑 → 直接訓練無意義，先恢復衝量。
- **全自動管線已在 rtm_build 容器背景起跑**（`/work/r5_pipeline.sh`，斷線不受影響）：
  1. `autolabel2.py`：掃完 unlabeled2017 全量（12.3萬張）。v2 改良：丟棄的也記到 `labeled/discarded.txt`，續跑不再重掃（本輪首跑仍需重掃先前 ~5.7 萬張一次）。
  2. `mkds5.py`：labeled 全量組 ds5，**val 沿用 ds4 的 1078 張檔名**（與 r3/r4 可比）。
  3. 訓練 seg_r5：與 r4 完全同參數（yolo11n-seg.pt 起訓、imgsz 256、epochs 80、batch 64、patience 20）。
  4. 匯出 ONNX 256/192/128 + best.pt 到 `/work/export_r5/`，完成寫 `r5_pipeline.done`。
- 預估總時程 4~6 小時（標註 ~2.5-3.5h + 訓練 + 匯出）。qwen 容器不受影響。
- 本機已掛監看器：完成/失敗/進度變化會通知。日誌：`r5_pipeline.log`、`r5_label.log`、`r5_train.log`。
- **驗收標準**：r5 mask mAP50 > r4 的 0.946 才換上線；預期資料量 21572 → ~30-34k 張。

### 💾 標註成果存回本機（使用者要求）
- 標註最終 **34,385 張**（21,572→34,385，+59%）。全量 tar（5.4GB）傳回本機 `訓練資料/` 合併：**圖 34,385 + 標籤 34,385，與 DGX 一致**。
- 坑：DGX `labeled/images` 有 996 張是指向**容器內路徑** `/work/dataset/val2017/` 的符號連結，主機與本機都解不開 → 用 readlink 映射回主機路徑抓實體檔補齊。未來打包記得 `find -type l` 先檢查。
- ds5 組裝完成（train 33,307 / val 1,078 沿用 ds4）→ r5 訓練中。

### 🌏 使用者指示：訓練資料需要更多亞洲人（r6 方向）
- 問題：COCO 以歐美場景為主，實際玩家是台灣人 → 分布不匹配影響實戰辨識。
- **雙軌補資料（r5 照跑不擋，亞洲增量進 r6）**：
  1. **CrowdHuman**（Megvii 蒐集、亞洲街景占比高、15k 張人群圖）：已在 DGX 背景下載（HuggingFace sshao0516/CrowdHuman，train 三個 zip 共 ~8.4GB）→ 之後跑同一套雙老師共識標註。⚠️ 授權**僅限非商業研究/教育、不得散布圖片**——圖只留 DGX，不進 repo。
  2. **實拍（價值最高，域完全匹配）**：本機新增 `訓練資料/實拍投入/`（已在 gitignore，含拍攝指南 README）。使用者丟照片/影片後說「處理實拍」→ `extract_frames.py`（已上 DGX，影片每秒抽 2 幀、統一 1280 長邊）→ autolabel2 自動標註 → 併入訓練。
- r6 時實拍資料量少可用重複取樣加權（複製 ×N 份進 ds6）。

### ✅ r5 完成並上線（c1d2973，版號 v29）
- 訓練 80 epochs 跑滿（~2.2h），**mask mAP50 0.952 > r4 0.946**（box 0.952；val 同 ds4 可比）。
- 模型抓回 `models/`（256/192/128 onnx + .pt），256/128 force-add 進 git 上線。
- 瀏覽器煙霧測試通過（r5 載入正常、偵測 2 人、C 路徑 mode=gpuC 正常）。
- ⚠️ C 桌機效益觀察：兩輪 A/B 結果 +1.7ms / -4.3ms，在量測雜訊內飄移 → 桌機上 C 增益不明確，最終以手機實測為準（有 ?noc 與自動退回保底）。
- CrowdHuman 標註已自動接棒（labeled_ch/，15k 張，供 r6）。
- 歷程：21,572→34,385 張(+59%) 只換到 mAP50 +0.006 → COCO 系資料報酬遞減明顯，**r6 重點是分布（亞洲人/實拍），不是數量**。

### 📊 單憑 C 的效能結論（v28 vs v27，桌機實測）
- 偵測單張延遲 26.5ms → 24.8ms，**省 1.7ms（~6.4%）**；純偵測吞吐 37.7 → 40.3fps。
- 桌機效益天花板已到：`sess.run` 8.5ms 返回後，12.2ms 是等 GPU 推論算完（output0 下載），這段省不掉；再把解碼/NMS 搬 GPU 也擠不出更多。
- **畫面 fps 不變（維持 ~141fps 等級）**：偵測在 worker 裡，C 改善的是命中判定遮罩的新鮮度，不是渲染流暢度。
- 手機未測：推論 ~40ms、若 readback 佔 5~10ms，C 可能壓到 ~35ms 左右（推估，以手機實測 `__segProf` 為準）。
