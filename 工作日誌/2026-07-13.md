# 工作日誌 — 2026-07-13

## 昨日狀態
- 遊戲上線 https://thortsui.github.io/Moblie-shooting-game/（輪廓分割 AI + 多人 + 顏色識別 + 開火特效 + 方法A節流 + 方法D Web Worker）。
- r4 模型（2.1萬張，mask mAP50 0.946）已上線取代 r3。
- C（GPU 後處理）在分支 experiment/worker-gpu 實驗中。

## 本日進度

### C 可行性探針（通過）
- onnxruntime-web 探針：`preferredOutputLocation:'gpu-buffer'` 成功、拿得到 `ort.env.webgpu.device`、output0/output1 皆為 GPUBuffer。→ C 的技術地基可行。

### ⭐ 關鍵量測：C 不值得做（數據推翻前提）
- 乾淨量測（主線程、無節流、r4@256）detect() 分解：
  - 前處理 **4ms**、推論 **39.9ms**、後處理 **0.3ms**、總 44.2ms（=19fps）。
- **後處理只有 0.3ms** → C（把後處理搬 GPU）就算歸零也只省 0.3ms，無意義。
- 先前「開銷 55ms 值得做 C」是**被方法A節流的 83ms 等待污染的錯誤數據**；真正瓶頸是**推論(40ms)**，C 不碰推論。
- C 唯一沾邊的是輸出 readback(~10ms 藏在推論內)，但要省它得 NMS+解碼全改 GPU shader（巨大工程/脆弱）換 ~10ms。
- **結論：C 經實測不值得（如量化般誠實止損）。真正加速偵測要降解析度或換小模型；且 A+D 已讓畫面 141fps。**
- 待使用者定奪：接受「C 不做」或堅持省 readback。

### r4 上線 + 背景標註
- r4（2.1萬張，mask mAP50 0.946 > r3 0.934）匯出 256/192/128 換上線（WebGPU 256 / WASM 128）。
- 背景標註持續（供未來 r5）。

### 🔌 電腦重開前存檔（2026-07-13）
- **所有變更已提交並推送遠端**：main 與 origin/main 同步；實驗分支 experiment/worker-gpu 也已推遠端備份。工作區乾淨、無未提交。
- **DGX 不受影響**（獨立機器）：標註 21572 張持續跑、qwen healthy。重開後用 `ssh -i ~/.ssh/dgx_claude ethan@192.168.0.126` 連。
- 本機備份齊全：模型在 `models/`、訓練圖在 `訓練資料/`、r3 訓練成果在 `訓練資料/seg_r2_訓練成果/`（註：資料夾名為r2但內容視實際）。
- **重開後接續事項**：
  1. 待使用者定奪 C 是否繼續（結論：實測不值得，後處理僅 0.3ms）。
  2. r5：背景標註累積更多後可再訓練。
  3. 本地測試伺服器需重啟：`cd 專案 && npx http-server -p 8777 -s`（線上版在 GitHub Pages 不受影響）。
  4. 線上遊戲 https://thortsui.github.io/Moblie-shooting-game/ 照常運作（跟本機無關）。
