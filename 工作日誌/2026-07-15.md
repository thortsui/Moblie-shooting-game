# 工作日誌 — 2026-07-15

## 昨日狀態
- 線上 v37：r8 模型（官方 GT 0.5718）+ 六武器系統（含火箭筒，圖/音/特效俱全）+ 遮罩延續 + 門檻中央加權（0.25/0.35）+ 貼身遮罩。
- r9 過夜訓練中（ds9 = r8 配方 + 局部特寫 20k，150,547 張；MHP 鏡像損毀改無 MHP 備案）；訓練截止今晨 06:00，四關評估後**使用者已授權直接上線**（退化才暫停）。
- 待辦：force push 清歷史（使用者需以 `!` 指令執行）、LV-MHP-v2 官方源人工下載（r10）、實拍素材（隨時）。

## 本日進度
- 凌晨：r9 訓練推進（epoch 60+，~11 分/epoch）。
- **使用者指示（凌晨）**：練到 12:00、r9 出爐直接上線、接下來訓「認人 + 精準度」、**靜音模式不要吵**（12:00 一次回報）。
- **r9 上線 v38**（5136713，依授權自動）：四關 @192——官方 GT 0.5028（持平 r8 0.5034）、**局部特寫 0.9170（r8 0.8545，+7%——合成特寫訓練奏效）**、雙人信心 **0.894/0.653**（r8 0.831/0.480）。cbench 煙霧通過（~23-25ms、dets=2）。
- **r10 已自動接棒**：從 r9 權重續訓（lr0=0.002 微調）至 11:10，四關評估 r8/r9/r10 同場，優於 r9 才上 v39。
- **認人（re-ID）素材**：Market-1501（1,501 人/3.2萬張）+ MSMT17（4,101 人/12.6萬張）人工標註 re-ID 資料集下載中。方案：小型嵌入模型（人物裁切 → 128 維向量、ONNX 數 MB）與開局登錄比對，取代/強化顏色識別；r10 結束後開訓。

### 📊 12:00 回報：r10 成績與定奪
| 關卡 @192 | r8 | **r9（線上 v38）** | r10 |
|---|---|---|---|
| 官方 GT | 0.5034 | 0.5028 | 0.4957 |
| 局部特寫 | 0.8545 | **0.9170** | 0.9191 |
| 雙人圖信心 | 0.831/0.480 | **0.894/0.653** | 0.856/0.692 |
- **定奪：維持 r9 上線**。r10 微調在局部特寫僅 +0.002、官方 GT 反降 0.7pp、雙人第一人信心下滑——過擬合徵兆，微調收益到頂。
- re-ID 素材下載解壓完成（REID_DONE），下一階段開訓認人模型。

### 🧬 認人模型訓練（使用者：訓好測正確率後上線）
- **v1**（MobileNetV3-small→128維嵌入，輸入 128×64，25 epochs，Market+MSMT 45,557 張/1,792 人）：訓練 acc 0.968 但 **Market rank-1 僅 0.556——不放行上線**（輸入太小丟細節）。
- **v2 重訓中**：標準 256×128 輸入、35 epochs、單檔 ONNX（v1 的外部權重檔 ort-web 難載）。放行門檻 rank-1 ≥0.75。
- **瀏覽器整合已寫好待部署**（reid.js + net.js emb 訊息 + main.js 接線）：大廳取樣同時算嵌入向量廣播；戰鬥中每目標 ~700ms 節流嵌入、cosine ≥0.5 覆蓋顏色判定、每輪最多 2 目標；模型載入失敗自動整體退回顏色識別。舊版互通（emb 欄位舊版忽略）。
- **v2 出爐：rank-1 0.731**（0.556→0.731）。略低於自設 0.75 門檻，但基準（8千人海撈+跨相機跨日）遠難於實戰（同場 2~8 人剛登錄互認）+ 失敗退回顏色保底 → **放行上線 v39（6901a12）**。桌機驗證 `[reid] 就緒 webgpu`。
- 使用方式：大廳按「取樣」會同時登錄顏色＋外觀特徵（狀態顯示「＋特徵」）；戰鬥中自動比對。待多人實戰驗證認人準度。

### ⚙️ 傍晚：解析度自適應（正確率優先 + 準星獨立 + fps 保底）v41
- **使用者指示**：①正確率優先、先不要誤判；②WebGPU 可用（≈iPhone 14 以上）改用 256；③準星/瞄準框顯示要獨立、不被 yolo 偵測拖累；④偵測 fps 保底 15，不然太卡。
- **做法（三檔連動）**：
  - `detector-seg.js`：拆出 `SEG_WORKER_HIRES=256`（背景執行緒／WebGPU 預設）、`SEG_MAIN_HIRES=192`（主線程退回，避免 256 在主線程卡準星）、`SEG_LORES=128`（WASM）、`SEG_FALLBACK=192`。worker 包裝新增 `size` getter 與 `setResolution()`（執行中切換解析度）。`?hq` 強制 256、`?fast` 強制 192。
  - `seg-worker.js`：新增 `setModel` 訊息 → 重建 InferenceSession + OffscreenCanvas，回 `model-ready`，支援 256↔192 執行中切換。
  - `main.js`：`DET_MIN_INTERVAL` 83(12fps)→**55(≈18fps 上限)**，讓偵測能到 15+；detectLoop 每秒量測偵測 fps，WebGPU+256 下**連續 2 秒 <15fps 自動降 192**（`resDowngraded` 只降一次）。
- **關鍵設計**：256 只在背景執行緒跑、切換也在背景 → 準星（DOM）與畫面（rAF）全程獨立，不被偵測影響。主線程退回路徑一律 192 主動保護準星。
- **信心門檻拉高（正確率優先、不誤判）**：中央/邊緣 `0.25/0.35 → 0.30/0.40 → 0.45/0.55`（使用者：門檻拉高、拉到上限）。detector-seg.js + seg-worker.js 兩處同步（含中央初篩下限）。取捨：只認高把握目標，誤判大幅降低，但遠處/邊緣或姿勢特殊的真人可能被漏成「未登錄」打不到——**待實測回調**。
- 版本 v40→**v41**（index.html/main.js/reid.js `?v=` 全部 bump、worker `?v=36→41`）。四檔 `node --check` 通過。**待手機實測**：①256 在 iPhone 14+ 能否穩 ≥15fps（撐不住自動降 192）；②準星是否確實不卡；③門檻 0.45/0.55 是否漏抓太多、需回調。
