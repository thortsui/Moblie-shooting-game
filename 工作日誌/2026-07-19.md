# 2026-07-19 工作日誌

## 使用者提案 → r14「解析度差距挖礦」

使用者洞察:「高畫質正確率高 → 困難圖用高畫質推、拿到準的結果、再降畫質當訓練素材」。
落地為 ML 正規做法(標籤本來就是人工的,不需要偽標;精華在「挑圖」):

- **挖礦**(`mine_hard.py`):全 ds11 訓練圖(去重後)各跑兩次推論——r12n@192 與 r11@384,
  與人工 GT 算遮罩聯集 IoU。**192 蒙(IoU<0.6)且 384 懂(IoU>0.75)** = 低解析度下有救的困難樣本。
  兩端都錯的不收(可能是標註噪音,加倍會害)。上限 3 萬張,按差距排序。
- **ds12**(`mkds12.py`):ds11 全量 + 困難樣本 ×2 加倍(總曝光 ×3)。
- **r14n**:困難樣本加倍 + r12s 蒸餾 + 劣化增強 @192 = 全部手段疊滿的 nano。
- 自動接棒:watcher 盯 r12 鏈 CHAIN-DONE → r14_chain.sh(挖礦→組ds→訓練→匯出→全場評估)。

## r12 鏈插曲

- Albumentations monkey-patch 簽名不合(這版會傳 transforms 參數)→ 修簽名重發。
- 第一次 crash 留下的空 `runs/seg_r12n` 佔住名字 → 真訓練被 ultralytics 改名到 `seg_r12n-2`,
  鏈尾匯出/評估會找錯目錄 → 已刪空殼 + 掛 watcher 在階段 2 開始時把 -2 改名回來。
- 目標校準(使用者):**0.7 及格、0.8 更好**。valgt@192 nano 物理極限誠實預估 0.55-0.60(s 模型),
  0.7 最近的門是 r11@384(0.649);遊戲域指標(parts 0.92/自家 val 0.956)早已達標。

## 待辦
- r12+r14 全鏈完成後:成績單對照 0.7/0.8 線回報,決定各階梯檔位模型(可能 n/s 混編)。
- 混合部署案(320/384→r11)仍在等使用者點頭。

## 正午衝刺重排(使用者 06:00 指令:練到 12:00、目標 0.85@192、靜音、沒素材就去找)

- **r12n 完訓**:60ep/6.5h(比估快)。鏈已自動開跑 r12s(12h+,正午前完不成)→ 殺掉重排。
- **新素材**:MHP v2 val 4,999 張多人(之前只用 train,這是現成未用的人工標註)→ labeled_mhpval,進 ds12 ×2。
- **正午鏈**(06:03 發車):挖礦(r12n@192 vs r11@384,約 1.5h)→ ds12(ds11+困難×3+mhpval×2)→
  r14n(從 r12n 微調 30ep lr0.005,est 至 ~11:45)→ 匯出 → 全場評估 → 12:00 交卷。
- **正午後自動接**:followup_chain = r12s(ds12 全配方 60ep)→ r13n(r12s 蒸餾)→ 決賽評估。0.85 長征繼續。
- 使用者速度提案記錄:「優先判斷紅色系物體附近」(人不會全藍,膚色暖色)——遊戲端 ROI 預過濾構想,
  待訓練窗口結束後評估實作(需實機測試,不與訓練混改)。
- 監控靜音化:只在 NOON-DONE/錯誤/12:05 到點喚醒。
