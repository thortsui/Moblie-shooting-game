# 工作日誌 — 2026-07-12

## 昨日狀態摘要
- 遊戲已上線 https://thortsui.github.io/Moblie-shooting-game/（單機 + 多人房間 + 顏色識別 + 斷線重連 + 開火特效 + PWA）。
- 已知待實測：顏色識別門檻、跨機連線品質。

## 本日回報問題
- [ ] 判定方框全部消失（疑似昨日「判定區放大」或「幽靈檔案」改動造成的回歸）
- [ ] 開啟遊戲前就先預載模型，減少按下按鈕後的等待
- 使用者再次詢問「如何訓練」→ 待回覆訓練流程說明

## 本日進度
### 方框消失排查與修正
- 用真人照片餵假相機重現：**最新版程式在桌機繪製正常**（頭圈/軀幹框/頭上血條都在），排除繪製邏輯回歸。
- 判定兩個最可能成因並都修掉：
  1. 多人模式顏色比對失敗 → 全員被判「未登錄」而未登錄框原本是近乎看不見的淡灰 → 改為**明顯的黃色虛線框**；同時放寬顏色比對（s/v 權重 0.4/0.15、門檻 0.55→0.75，容忍光線差異）。
  2. 部分 iPhone WebGPU 初始化成功但推論失敗 → detect 連續錯 6 次自動降級重建 WebGL 後端，畫面提示切換。
- 待使用者回報：若還是沒框，看畫面是否出現黃虛線（=顏色問題）或完全沒有（=偵測問題）。

### 模型預載
- 頁面載入即背景預載模型（不等按鈕），主選單顯示載入進度；實測按下單機到進入戰鬥 **189ms**（原本數秒）。預載失敗會在進遊戲時自動重試。

### 人物專屬 ID 制 + 修正血條打不完
- 問題：偵測器的追蹤 ID 在快速重偵測時頻繁跳號，每次跳號被當新目標重置滿血 → 血條永遠打不完。
- 修法：TargetRegistry 改為「人物檔案」制。偵測追蹤 ID 只是暫時代號，透過(1) 位置接續（<1.5 秒內位置重疊視為同人，解決掉鎖跳號）(2) 衣服顏色比對（出鏡較久回來）映射回穩定的人物專屬 ID（P1、P2… 整場不變）。血量掛在人物檔案上，取代昨天的幽靈機制。顏色檔案用環狀色相插值緩慢更新以適應光線。
- 測試：追蹤 ID 11→22→33→44 連跳映射回同一 P1、血量累積 75→50→25→0 擊倒（只建 1 檔）；紅藍雙人不混淆各自獨立血量；紅衣出鏡 3 秒回來仍認回 P1 帶 50 血；真人雙人畫面穩定顯示 P1/P2。已上線。

### DGX / 全新 YOLO（待辦，未動作）
- 使用者提到有一台 DGX（訓練 YOLO 用），要在其 project 資料中找一隻全新的 YOLO 接進遊戲。
- 現況：**未取得任何 DGX 金鑰/連線資訊**；使用者指示「不能動除了工作資料夾以外的東西」。故無法連線 DGX。
- 已與使用者確認走法 A：由使用者把新 YOLO 權重檔（需為 **pose 姿態版**，如 yolo11n-pose）複製進 `Phone gun fight/models/`，再由我轉 ONNX/TF.js 接進 detector.js（該檔已留抽換介面）。等待檔案。

### 訓練問題回覆
- 使用者再問「如何訓練」→ 已回覆：現階段問題皆非訓練能解；附未來訓練流程（收資料→標註→ultralytics 微調（DGX）→匯出 ONNX/TF.js）供之後參考。
