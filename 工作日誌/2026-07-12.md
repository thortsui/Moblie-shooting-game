# 工作日誌 — 2026-07-12

## 昨日狀態摘要
- 遊戲已上線 https://thortsui.github.io/Moblie-shooting-game/（單機 + 多人房間 + 顏色識別 + 斷線重連 + 開火特效 + PWA）。

## 本日進度（上半天：遊戲修正）
### 方框消失排查與修正
- 真人照片重現：繪製正常，排除繪製回歸。修兩處：未登錄框改明顯黃虛線、放寬顏色比對門檻；WebGPU 推論連續失敗自動降級 WebGL。
### 模型預載
- 頁面載入即預載模型，按下到進戰鬥 189ms。
### 人物專屬 ID 制 + 修正血條打不完
- TargetRegistry 改人物檔案制：追蹤 ID 只是暫時代號，用位置接續（<1.5s）+ 顏色映射回穩定專屬 ID（P1、P2…）。修正追蹤器跳號導致血條重置。已測通、上線。
### 安全事件：DGX 手冊誤入公開 repo + 隱私資料夾
- DGX 手冊誤推公開 repo（無私鑰無密碼，低風險）。已從最新版移除；新增 `隱私資料/`（整個 gitignore）集中敏感檔。**待辦**：手冊仍在遠端 git 歷史，徹底抹除需 force push（auto mode 擋，待授權）。

## 取得 pose YOLO（已完成）
- 連上 DGX（唯讀盤查發現無 pose 模型）。使用者決策用預訓練 pose 版。DGX 新建 `~/pose-export/` 匯出 `yolo11n-pose.pt`+`yolo11n-pose.onnx` 抓回 `models/`。訓練路徑寫在 `models/README.md`。

---

## ⭐ 自主任務：訓練即時人物「輪廓」模型（進行中，每小時回報）

**目標**：訓練小而快的模型，即時標出人物外輪廓（含各種角度、半身）。過關條件（fps 為手機端，暫用桌機代理）：10fps 完美=最高分、7fps 完整=4分、5fps 完整=3分，須 ≥3 分。「提高準度、提高效率、減少效能」。

**方向裁定**：使用者說「架 RTMPose」但過關條件是「外輪廓」= 實例分割（非關鍵點）。以過關條件為準 → 做人物實例分割。RTM 家族理解為輪廓老師之一。

### 回報 #0（kickoff，環境建置）
- **DGX 獨立環境完成**：新建 `~/rtm-outline/`，以 `nvcr.io/nvidia/pytorch:26.01-py3` 為基底啟動獨立容器 `rtm_build`（不動既有容器/qwen），已存成映像 `rtm-outline:latest`。torch 2.10 + CUDA 在 GB10 正常。
- **兩個輪廓老師驗證通過**（不同架構的共識配對）：
  - 老師A：yolo11x-seg（CNN）→ 人框+粗遮罩，GB10 上 **18ms/張**。
  - 老師B：SAM2（Transformer）以 yolo 人框當 prompt 精修邊緣 → **81ms/張**。
- **資料集**：改用合法公開的 COCO（各種角度/半身/遮擋，符合條件），不爬 Google（版權/品質）。val2017（5000 張）背景下載中，先建管線再擴量。
- **共識標註腳本** `autolabel.py` 已寫：兩老師遮罩 IoU≥0.85 採用 SAM2 精修輪廓為答案、輸出 YOLO-seg 單類 person 標註；分歧樣本另存待 Claude 仲裁。
- 現實限制（自行處理、不卡使用者）：手機 fps 無法遠端量測 → 用桌機 Chrome onnxruntime 當代理基準，最終需使用者手機驗一次。

**下一步（回報 #1）**：確認資料下載完成 → 跑共識標註 → 檢視標註品質與保留率 → 開始訓練 nano 學生模型。

### 回報 #1（標註完成 + 首次訓練啟動 + 圖片存回本機）
- **共識標註完成**：val2017 5000 張 → 雙老師（yolo11x-seg + SAM2）IoU≥0.85 共識，**保留 996 張**、丟棄 1591 張（分歧）、其餘無人。標註速度 ~70ms/張。保留率偏嚴格（品質優先）。
- **✅ 訓練圖片已存回使用者電腦**（本次新需求）：整理成真實圖檔（原本 symlink 會壞），抓回 `訓練資料/`（996 圖+標籤，160MB，已 gitignore）。autolabel 腳本也已改為複製真實檔，後續每批都會抓回保存。
- **切分**：train 897 / val 99，`ds/data.yaml`（單類 person）。
- **首次訓練 seg_r1 啟動**：yolo11n-seg nano、imgsz 320、batch 32、60 epochs，背景跑（GB10 上 ~9 it/s，數分鐘完成）。首個 val 指標 mask mAP50≈0.57（僅暖身，資料量小）。
- **衝量中**：背景下載 COCO train2017（118k 張），下一輪標註擴到使用者要求的 ~10000 張再訓練 r2。
- 現實限制重申：手機 fps 無法遠端量，最終用桌機 onnxruntime 代理 + 使用者手機驗一次。

**下一步（回報 #2）**：取 seg_r1 結果做首次 fps/品質基準；train2017 下載完成後跑大批共識標註→抓回本機→訓練 r2。
