# 2026-07-20 工作日誌

## 凌晨:夜間接力鏈過半(續 07-19 復原後排程)

- **r13n 蒸餾完訓**(60ep 跑滿,~00:3x):自家 val mask mAP50 **0.911** / 50-95 0.641,
  對照老師 r12s(0.947/0.717)——nano 保住約 96% 的 mAP50 力氣,推論 0.2ms/張。
- 鏈進入匯出(onnx 128/192/256)+ 全場評估(eval_r12:r9/r12n/r12s/r13n/r14n × 3 檔 × valgt/parts),
  完成後 FOLLOWUP-DONE → r15_chain 自動接 **r15n@640**(40ep 無劣化增強,est ~12h,今日中午)。
- 昨日戰果回顧:**r12s 奪 192 新王(valgt 0.535 > r9 0.503)**,r13n 全場成績待評估出爐,
  重點看它能否在 192 檔以 nano 身材逼近 r9。

## 全場評估出爐(eval_r12,五模型 × 128/192/256)

### 192 檔位決賽總表(valgt / parts)
| 模型 | valgt@192 | parts@192 | 判定 |
|---|---|---|---|
| **r12s(s,4×容量)** | **0.5352** | **0.928** | 🏆 192 總冠軍(雙基準全勝) |
| r9(n,256訓練) | 0.5028 | 0.917 | nano 組冠軍(不變) |
| r12n(n,192直訓) | 0.4662 | 0.9069 | — |
| r13n(n,r12s蒸餾) | 0.4631 | 0.9081 | ❌ 蒸餾無增益 |
| r14n | 0.456 | 0.8848 | ❌(昨日已判) |

- **教訓④:蒸餾無增益**——r13n(r12s 老師)valgt 0.4631 ≈ r12n 0.4662(還微輸),parts 亦僅持平。
  nano 在 ds12@192 的天花板 ~0.46,r9 的 256 訓練下放(0.503)仍是 nano 正解。蒸餾此設定不值得再花 GPU 時。
- @256:r12s valgt 0.5857 全場最高但 parts 崩 0.7665 → 256 檔仍由 r9(0.5756/0.9187)守。
- @128:r12s 0.4268/0.9226 雙冠。
- **部署建議雛形**:128/192 用 r12s(若手機跑得動 s)否則 r9;256 用 r9;320/384 用 r11。

### r15n@640 時程失準(重要)
epoch 1 實測 ~1.4s/it × 3300 it ≈ **75 分鐘/epoch**,40ep ≈ **50 小時**(原估 12h,差 4×)——
ds12 全量 21 萬張 @640 的真實代價。batch 64 僅吃 11.1G(可加大,但疑似 dataloader 瓶頸)。
**未擅自中斷**,照跑中;若使用者要縮短:砍 epochs(20ep ≈ 25h)或抽樣資料集,醒來定奪。

## 使用者改向:停 s 系列、控制變因實驗、資料稽核

1. **指令:先不練 s 系列;要「除了訓練畫質外全部和 r9 一樣」的對照組**(r15n 的 ds12+無增強+40ep 不符,
   epoch 1 未完即殺,無損失)。
2. **r9_640 控制變因實驗**:直接拿 `runs/seg_r9/args.yaml` 當 cfg、只改 imgsz=640。
   - ⚠️ 事故:這版 ultralytics cfg 內含 `save_dir`,CLI 的 name 覆寫無效 → 差點寫進 seg_r9 本尊目錄。
     幸有事前備份 best.pt(`/work/seg_r9_best_backup.pt`),epoch 1 未完即攔下,權重無損,args.yaml 已修復。
     **教訓:用 cfg= 重訓必須做獨立 cfg 副本改 name+save_dir,不能靠 CLI 覆寫。**
   - 已用獨立 `/work/r9_640_cfg.yaml` 重發車,確認寫入 seg_r9_640,14h 上限(和 r9 同),
     完訓自動匯出+eval_r9_640(r9 vs r9_640 @192-640 全曲線)。監控已切換至此鏈。
   - 順手清掉殭屍 watcher(等 r12 CHAIN-DONE 要發已淘汰 r14 鏈的)。
3. **使用者懷疑:訓練資料有問題** → 稽核 ds11 新增的 8 個轉檔來源(LIP/AISeg/fashion/ATR/P3M/EP/OCH/SLY,
   多為人體解析/去背類,若轉檔把衣服/臉遮罩當整人 = 系統性毒藥)。
   - 基本統計:無空標、無壞多邊形;fashion 覆蓋率 26.7% 偏低(單人卻只蓋 1/4 畫面)待驗。
   - **使用者提案:不看圖省 token,「給 YOLO 看」**——用 r11 對每來源推論,算 GT vs 預測的 IoU+面積比
     (面積比 «1 = 只標部分)。GPU 被訓練吃滿改 CPU 跑(每來源 100 張)。
   - **稽核結果:標註品質無罪**。11 來源 IoU 中位 0.78-0.97、面積比全在 0.94-1.01——
     沒有「只標衣服/臉」的轉檔毒藥;fashion 洗清嫌疑(IoU 0.946/面積比 0.97,低覆蓋率只是人小)。
     最弱的是 cocogt(IoU 0.782、13% 低於 0.5)= COCO 粗多邊形+難場景,非轉檔問題。
     ⚠️ 判官偏誤保留:r11 本身是 ds11 訓的,同意度高不完全等於標註正確,但面積比指標可排除系統性部分標註。
   - **新頭號嫌疑:組成失衡而非標註錯誤**——ds11 新增 ~4.7 萬張全是單人圖(lip/aiseg/fashion/atr/p3m/ep/sly),
     稀釋了多人難例(valgt 是多人地獄考卷),與 r7 教訓「多人優先、單人控量」方向相反。
     驗證法:mkds13 = ds9 + 新單人來源砍半,或等 r9_640 對照排除配方因素後再議。

## 控制變因實驗矩陣成形(使用者加碼:r9s)

以 r9 為基準,單變因隔離:
| 實驗 | 唯一改動 | 狀態 |
|---|---|---|
| r9(基準) | — | 已有(valgt@192 0.503) |
| **r9_640** | imgsz 256→640 | 訓練中,~明早 08:30 完+eval |
| **r9s** | model n→s | 已排隊,r9_640 收官自動接力(+14h) |

三者同 ds9、同增強、同 14h 預算。對照可回答:畫質值多少分、容量值多少分(在乾淨 ds9 上,
排除 ds12/劣化增強干擾)。r9s vs r12s 同場評估亦可反推 ds12+劣化增強配方的淨貢獻。
狀態腳本已改為兩鏈接力版,監控續盯。

## 傍晚:r9_640 完訓但收尾崩潰,鏈斷 10 小時後救回

- **r9_640 訓練其實跑完了**:26 epochs / 13.99h(撞 14h 上限),自家 val mask mAP50 **0.975** / 50-95 **0.830**,
  best.pt 完好(08:24 存檔)。
- **事故**:訓練程序結束時 dataloader teardown 崩潰(`terminate called without an active exception`,
  core dump,疑 torch nightly 舊病),`set -e` 把鏈腳本一起帶走 → 匯出/eval/DONE 旗標全沒跑,
  GPU 從 08:29 閒置到 18:20(~10h 白等)。**教訓:鏈腳本別把 teardown 崩潰當訓練失敗,
  應改判「best.pt 存在即成功」再接後續步驟。**
- **救援(18:20)**:補發 `r9_640_resume.sh`(匯出 onnx 256/384/512/640 → eval_r9_640 → R9640-DONE)。
  昨晚的 r9s watcher(PID 41134)其實還活著在輪詢旗標,誤發的重複 watcher 已殺,無撞車。
- 時程:eval 約 1h 內出爐(r9 vs r9_640 @192-640 全曲線)→ r9s 自動發車(14h)→ 明早 ~09:00 完訓。

## eval_r9_640 出爐:畫質問題實錘,r9_640@640 valgt 0.7339 破全場紀錄

| imgsz | r9 valgt | r9_640 valgt | r9 parts | r9_640 parts |
|---|---|---|---|---|
| 192 | **0.5028** | 0.4465 | **0.9163** | 0.8997 |
| 256 | **0.5755** | 0.5317 | 0.9188 | **0.9228** |
| 384 | 0.6452 | 0.6392 | 0.761 | **0.9262** |
| 512 | 0.6672 | **0.7022** | 0.381(崩) | **0.9174** |
| 640 | 0.637 | **0.7339** 🏆 | 0.0892(崩) | **0.9049** |

- **控制變因答案:訓練畫質 256→640,同配方同資料,@640 差 +0.10 valgt(0.637→0.7339)**,
  且 parts 全程 0.90+ 不崩(r9 高解析 parts 雪崩)。教訓②「甜蜜點」再驗證:低檔(192/256)仍是 r9 勝。
- 0.7339 = 歷來 valgt 最高分(前紀錄 r12s@256 0.5857),0.85 目標的路確定在高解析。

## 傍晚加單:全模型大會考(使用者指令)

- 使用者指令:**所有訓練過的模型 @192/256/320 全部重考,含舊版(r1–r8),統一出一張 Excel**。
- 部署 `eval_all.py`:runs 下 16 個有 best.pt 的模型(r1–r9、r9_640、r10–r14n)× 3 檔 × valgt+parts,
  共 48 場,增量寫 `eval_all_192_320.json`。插隊在 eval_r9_640 之後、r9s 開訓之前
  (殺舊 watcher,`eval_all_chain.sh` 完考後自動重啟 r9s 接力)→ **r9s 延後數小時,約明日中午完訓**。
- 本機掛 persistent Monitor 盯進度,完考後產 Excel 交付。
- **大會考 10 分鐘跑完(48/48)**,r9s 已自動發車(epoch 1/200,14h 上限,~明日中午完訓)。
- 產出 `模型評估總表_192_256_320.xlsx`(專案根目錄,綠底=欄冠軍)。看點:
  - **@192 雙冠 r12s**(valgt 0.5351 / parts 0.9295);@256 valgt 亦 r12s 0.5858,parts 由 r11 0.9233 微幅奪下。
  - **@320 valgt 冠軍竟是 r8(0.6174)**,小勝 r9 0.6156/r10 0.6129/r11 0.6119——r11 的 320 主場優勢不成立,
    320 檔位選擇需重新檢視;parts@320 則是 r9_640 0.9222 最穩。
  - r12n/r13n/r14n 高解析 parts 雪崩(劣化增強系全滅),舊版 r1–r7 全面墊底,無翻案。

## s vs n 推論速度實測(DGX CPU、ONNX 4緒、50輪中位數)

| 檔位 | nano(r13n) | small(r12s) | 倍率 |
|---|---|---|---|
| @192 | 4.49 ms | 11.19 ms | 2.5× |
| @256 | 6.53 ms | 20.5 ms | 3.1× |

- 理論:參數 2.8M vs 10.1M(3.5×)、FLOPs 3.4–3.6×;小解析度固定開銷稀釋差距。
- **s@192 成本 ≈ n@320,而 r9@320 valgt 0.6156 > r12s@192 0.5351 → 同算力下「n+高解析」勝過「換 s」**,
  待 r9s 完訓做最終容量 vs 畫質對照。手機端:換 s 舊機(WASM)出局、新機(WebGPU)勉強。

## 裁灰邊(長方形輸入)驗證:免重訓,準度微升+速度 1.19×

- 使用者提議「切掉灰邊重訓(r9 配方@256)」→ 驗證後**免重訓**:全卷積模型不在乎畫布形狀,
  且 ultralytics val 本來就是 rect 模式(r9 現有成績已是無灰邊成績);rect=True 重訓反會關 mosaic(配方陷阱)。
- 零成本實測(r9 原權重,parts 直式子集 237 張,CPU):
  方形 256+灰邊 0.9157 vs **裁灰邊 256×160 0.9194(微升)**;CPU 速度 5.72→4.80ms(**1.19×**,理論 1.6×,
  小模型 CPU 固定開銷稀釋;手機 WebGPU 預期 1.2~1.6×)。
- 已匯出 `/work/seg_r9_256x160.onnx`。**落地缺口:detector-seg.js 前處理/遮罩解碼寫死正方形**,待使用者點頭改遊戲端。

## 遊戲端無灰邊上線(使用者:「平常正常使用時也不要灰邊」)

- 方案:**dynamic ONNX**(`seg_r9_dyn.onnx`,export dynamic=True)單一檔案吃任意 /32 尺寸,
  letterbox 改「長邊=檔位、短邊貼相機比例向上取 /32」→ 直拍灰邊 44%→<6%;
  解析度階梯照舊,但**換檔零重載**(同檔案只改輸入尺寸)。
- 改動:`detector-seg.js`(階梯全指 dyn 模型、主線程 rect letterbox、segConfTh 改 W/H)、
  `seg-worker.js`(rect letterbox、動態畫布、setModel 同檔零重載、gpu-buffer 執行期失敗自動退回重試)、
  index.html v=46。GPU shader 本來就吃動態 mh/mw 不用改。
- 測試(`_test_rect.html`,canvas captureStream 假相機,Playwright + 桌機 Chrome WebGPU):
  直式 480×854→輸入 192×128/256×160、橫式→128×192,偵測正常、剪影貼身、座標無偏移、
  中心命中 true、gpuC 路徑正常、setResolution(256) 零重載。PASS。
- 已 commit + push 上線(GitHub Pages)。手機實測 fps 增益待使用者回報。

## 色塊化加速提問(使用者)→ 否決:卷積運算量只由輸入尺寸決定,與內容無關;
色塊化不省算力、多花前處理、且畫風偏離訓練域傷準度。真正加速=降解析度(已有階梯)、int8 量化、ROI 裁切(待試)。

## 模型世代選單上線(使用者:「網站加一格自由換各代,預設最強」)

- 15 代全部補匯 dynamic ONNX(DGX CPU 批次,`export_all_dyn.sh`),連 r9 共 16 檔進 web repo(~200MB)。
- 前端:`index.html` 主選單新增 #genSelect(16 選項);`detector-seg.js` 加 SEG_GENS 清單+
  localStorage 記憶(預設 r9=綜合最強);**階梯 cfg 的 model 改 getter**,換代後升降檔跟著新代不回跳;
  `main.js` initGenSelect() 熱切換(worker setModel 跨檔重建 session,主線程路徑退回 reload)。v=47。
- Playwright 驗證:16 選項渲染、localStorage 記憶、r12s↔r9 熱切換+狀態回饋、?gen= 測試參數。
  彩蛋實測:同一張橫式雙人圖,r9 抓 1 人、**r12s 抓 2 人(0.70)**——大模型準度優勢直觀可見。

## 待辦
- eval_r12 出爐:更新 192 總表,決定 192 檔位部署模型(r12s 準度 vs r13n 速度)。
- r15n@640 完訓後:r11/r15n 高解析對照(384/512/640),高畫質階梯第一階驗收。
- 懸而未決:混合部署案(320/384→r11)、紅色 ROI 提速構想(訓練窗口後實機測)。
